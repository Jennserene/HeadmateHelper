import React from 'react';
import { View } from 'react-native';

// This component is not currently used. Perhaps use it in components/main/system/Intro.js?
const ExplainMarkdown = (props) => {
  return (
    <View>
      
    </View>
  );
};

export default ExplainMarkdown;