import React from 'react';
import { View, Text, StyleSheet, Pressable } from 'react-native'

const Settings = (props) => {
  return (
    <View>
      <Text>Sorry, this feature has not been implimented yet.</Text>
    </View>
  );
}

export default Settings;